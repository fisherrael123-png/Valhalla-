知识实体：
用于记录知识点的唯一标志（entity_id）。
同一个知识点在不同资料、不同作者或不同上下文中可能存在多种表述形式，因此需要使用统一的 entity_id 对其进行规范化指代。
否则，系统在整理、合并、引用和建图过程中容易出现知识指代漂移。
即同一知识点被拆分为多个节点，或不同知识点被错误合并为同一节点，从而破坏知识图谱中知识节点的唯一性、可追溯性和关系一致性。

注意与resource的区别：
entity是一个知识的抽象实体；
entity_id是这个抽象实体的名字；
一个知识的各种表述，是这个知识实体的抽象表示；
resource是知识的载体。