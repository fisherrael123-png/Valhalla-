# Exit the Knowledge Base

1. Confirm that the target Wiki exists.
2. Restore `.valhalla/kb_status.md` to:

   ```yaml
   kb_status: idle
   target_wiki_path: null
   ```

3. Report `os_state` and `current_state`.
