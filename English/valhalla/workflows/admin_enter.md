# Enter Admin State

1. Locate the system status file:
   - Windows: `%USERPROFILE%\.codex\valhalla\os_status.json`
   - macOS/Linux: `~/.codex/valhalla/os_status.json`
2. Set the system status file's `status` entry to `admin`.
3. On success, reply `Entered admin state`.
