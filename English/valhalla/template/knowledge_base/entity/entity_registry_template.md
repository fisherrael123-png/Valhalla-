# entity_registry

This is the **human-readable projection** of the current knowledge base's Entity registry.

Authoritative machine file: `.registry/machine/entity_registry.yaml`

If a conflict occurs, YAML takes precedence. Never overwrite YAML from this Markdown file.

| entity_id | name | type | status | content_file | resource_refs |
|---|---|---|---|---|---|
